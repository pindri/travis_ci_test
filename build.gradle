apply plugin: 'java'

apply plugin: 'application'

mainClassName = 'main.calc.Calc'

repositories {
    mavenCentral()
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

dependencies {
    testImplementation "junit:junit:4.12"
}

jar {
    archiveBaseName = 'gs-gradle'
    archiveVersion =  '0.1.0'
}
